<%= form_for(@press_release) do |f| %>
  <% if @press_release.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@press_release.errors.count, "error") %> prohibited this press_release from being saved:</h2>

      <ul>
      <% @press_release.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

	<fieldset>
		<p>
			<%= f.label :title, "Press Release Title" %><br />
			<%= f.text_field :title %>
		</p>
		<p>
			<%= f.label :contents %><br />
			<%= f.text_area :contents, :class => "ckeditor" %>
		</p>
		<p>
			<%= f.label :publish_date %><br />
			<%= f.date_select :publish_date %>
		</p>
	</fieldset>

  <p class="buttons">
    <%= f.submit %>
  </p>
<% end %>
